[package]
name = "NumCore"
version = "0.1.0"
edition = "2021"

[[bin]]
name              = "NumCore"
test              = false
bench             = false

[profile.release]
opt-level = "z"   # minimise code size
lto       = true
panic     = "abort"

[profile.dev]
panic = "abort"